import 'package:flutter/material.dart';

class AchivScreen extends StatelessWidget {
  const AchivScreen({ key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}